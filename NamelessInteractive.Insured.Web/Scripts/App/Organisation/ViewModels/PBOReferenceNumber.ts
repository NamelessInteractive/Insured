module InsuredApp {
    export module ViewModels {
        export module Organisation {
            export class PBOReferenceNumber {
                PBOReferenceNumber: string;
                constructor(pboReferenceNumber: string) {
                    this.PBOReferenceNumber = pboReferenceNumber;
                }
            }
        }
    }
} 